void main() {
  // int i = 0;
  int n = 20;

  // i = 0; // Initialisierung
  //while (i <= n) { // Bedingung
  //  System.out.println(i + "² = " + (i * i)); // Anweisung(en)
  //  i++; // Update
  //}

  // for (Initialisierung; Bedingung; Update) {
  //        Anweisung(en)
  // }

  System.out.println("========================");

  for (int i = 0; i <= n; i++) {
    System.out.println(i + "² = " + (i * i));
  }
}
